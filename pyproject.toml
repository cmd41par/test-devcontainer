[project]
name = "test-devcontainer"
version = "0.1.0"
description = "Devcontainer stages sameples"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest",
    "ipykernel",
    "matplotlib",
    "pandas"
]